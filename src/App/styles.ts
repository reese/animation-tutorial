import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  pointer: {
    bottom: 0,
    fontSize: 50,
    height: '45%',
    position: 'absolute',
    textAlign: 'center',
    width: '100%',
  },
})
